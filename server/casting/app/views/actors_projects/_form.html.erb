<%= form_with(model: actors_project, local: true) do |form| %>
  <% if actors_project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(actors_project.errors.count, "error") %> prohibited this actors_project from being saved:</h2>

      <ul>
      <% actors_project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
